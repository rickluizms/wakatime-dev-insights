[pytest]
asyncio_mode = auto
addopts = -v --strict-markers
testpaths = tests
pythonpath = .
env =
    DATABASE_URL=sqlite:///:memory:
    WAKATIME_API_TOKEN=fake-token
    API_KEY_READONLY=test-api-key
    ENABLE_EMAIL_INSIGHTS=false
    ENABLE_EMAIL_INSIGHTS=false
